{
  "name": "data.table",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/dereckmezquita/data.table.git"
  },
  "author": {
    "name": "Dereck Mezquita",
    "email": "dereck@mezquita.io"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn run clean && tsc",
    "publish": "yarn run build && yarn publish",
    "link": "yarn build && yarn link",
    "format": "prettier --write .",
    "test": "jest"
  },
  "prettier": {
    "semi": true,
    "trailingComma": "none",
    "singleQuote": true,
    "printWidth": 80,
    "tabWidth": 4,
    "useTabs": false,
    "overrides": [
      {
        "files": "*.yml",
        "options": {
          "tabWidth": 2
        }
      }
    ]
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.26",
    "husky": "^9.0.11",
    "jest": "^29.7.0",
    "prettier": "^3.3.3",
    "ts-jest": "^29.2.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
